<%= form_for([@ticket.event, @ticket], html: {remote: true }) do |f| %>
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-labelledby="myModalApuesta" aria-hidden="true" >×</button>
    <h3 id="myModalLabel">Apuesta</h3>
  </div>
  <div class="modal-body">
    <p>
      <%= f.label :apuesta %>
      <%= f.text_field :apuesta %>
    </p>   
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Cerrar</button>
    <%= f.submit "Crear Apuesta", :class => "btn btn-primary" %>
  </div>
<% end %>